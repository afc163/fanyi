# 说明:本项目测试通过 fork CLI 子进程运行 (tests/index.test.ts),
# 覆盖率由 c8 统计子进程;部分错误处理与防御性分支(畸形响应、网络异常)
# 难以通过集成测试稳定触发,故适度放宽 patch/project 门槛。
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 6%
    patch:
      default:
        target: 55%
